import styled from "styled-components";

export const SearchBar = styled.div`
    background-color: var(--Bright);
    position: fixed;
    display: flex;
    max-height: 10vh;
    right: 0;
    overflow: auto;
    
`